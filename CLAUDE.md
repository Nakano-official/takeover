# takeover — Claude Code コンテキスト

このファイルはClaude Codeがプロジェクトを理解するためのコンテキストです。
作業を始める前に必ずこのファイルを読んでください。

> **あわせて参照**：相談で決まった仕様・経緯は `docs/spec/decisions.md`（決定ログ）、
> 未対応の改善点は `docs/spec/backlog.md` にある。仕様の判断はこの2つも確認すること。

---

## プロジェクト概要

**龍谷大学 障がい学生支援室 シフト管理・業務DXシステム**

障がい学生支援室のアルバイトスタッフ（ノートテイカー）のシフト管理、
欠員補充業務の自動化、および勤怠整合性チェックを行うWebシステム。

- **リポジトリ名**: takeover（`Nakano-official/takeover`。旧 `ryukoku-support-shift`）
- **可視性**: Public。**実データ・個人情報を含むものは絶対にコミットしない**
  （勤怠CSV・時間割PDF・実データのスクリーンショットは `.gitignore` で除外済み）
- **画面に出す名称**: 「支援室シフト管理」（利用者向けの呼称。リポジトリ名とは別物なので、
  `code.js` のタイトルや `Setup.js` のスプレッドシート名は変更しない）
- **開発期間**: 〜 2026年9月17日（授業開始日）

---

## 解決する課題

### 課題1：欠員補充フローの非効率

現状フロー：
1. 学生スタッフが Google Classroom に欠勤連絡を投稿
2. **問題**: バナー通知に本文なし・イベント投稿に埋もれる（Pull型）
3. 職員が気づいた時点で人材プールのスケジュール表を目視でスクリーニング
4. 職員が候補者に個別電話
5. 全員断った場合 → 残り1人が単独テイク（例外）/ 最終手段で職員が対応

**原則**: テイク業務は2人体制、移動/介助業務は1人体制。いずれも欠員 = 必ず補充アクションが必要
（移動/介助は1人体制だが、休憩時間内の移動介助もあるため1人欠員でも補充必須）。
補充できない場合は教務課または支援室スタッフが対応する。

### 課題2：勤怠整合性チェックの手作業（※ 今回のリリース対象外・D41）

現状フロー：
1. クォーター開始時に全勤務予定をGoogleカレンダーに登録
2. イレギュラー発生時（超過・早退・休校・欠員・代行・担当変更）に
   利用学生 or 職員がGoogleカレンダーを手動修正（修正漏れは比較的少ない）
3. 月末に学生スタッフが大学の労務管理システムで勤怠登録（期限：月末）
   → **学生側の記入ミス・登録漏れが多く、照合でよく引っかかる**
4. 「勤怠一覧データ ↔ Googleカレンダー」を**目視で突き合わせ**て承認
   → 教務課 → 支援室の順に確認日を設けて二段階でチェックする

**問題の本質**: 学生の勤怠登録（労務管理システム）に記入ミス・漏れが多く、それを
カレンダーと人手で目視照合して見つけている。照合作業そのものも手作業で重い。

---

## システムが解決すること

> ⚠️ **今回のリリースは機能Aだけ**（D41）。機能Bは「今後作る予定はあるが、いまは作らない」
> として `future/feature-b/` へ退避してある（`clasp push` の対象外＝本番に存在しない）。
> 以下の機能Bの記述は**将来の設計メモ**として読むこと。残課題は `future/feature-b/README.md`。

### 機能A：欠員補充の自動化
- 欠員情報をPush通知で職員・スタッフに即時伝達
- 人材プールから該当コマの空きスタッフを自動スクリーニング
- 候補者へ一括で代行依頼通知を送信（Google Chat Webhook）
- 回答を集約してダッシュボードに表示
- **最初に「承諾」した候補者へ先着順で自動確定**（職員は通常介在しない）。
  誰も承諾しないまま**締切（授業開始30分前・D21）**に達したら、システムは**募集を閉じるだけ**で
  `result` は書かず、職員へ決着を要求する（**募集クローズ＝システム／決着＝職員**・D22）。
  「1人テイク／職員対応」の決定は人員配置の判断なので必ず職員が manage 画面で行う。
  ※ 同時承諾の競合に備え、確定処理は LockService 内で原子的に行う（docs/spec/decisions.md D1）
- 代行記録をGoogleカレンダーに自動反映

### 機能B：勤怠整合性チェックの自動化
- Googleカレンダーの勤務予定と勤怠登録データを自動照合
- 差分（登録漏れ・時間不一致など）を一覧でハイライト表示
- 月末前にリマインダー通知を自動送信
- 照合済みレポートを教務課と共有、承認ボタン1つで完結

---

## 業務ドメイン知識

### テイク／移動・介助業務について
- ノートテイク業務は**原則2人体制**
- 1人が欠員 → 補充が必要（補充できない場合のみ例外的に1人テイク）
- 1人体制のテイクに欠員 → 補充できない場合は最終手段で職員が対応
- **移動/介助業務は1人体制**。ただし休憩時間内の移動介助もあるため、欠員時は必ず補充が必要
- **授業のあいだの移動介助は時限に収まらない**ので、`periods` に専用の枠として持つ（D33）。
  空きコマもテイクとは別に集める（`staffs.assist_slots`・D32）
- 移動介助は**授業の前と後の両方**にある（D35・職員確認 2026-09-17）。
  介助の担当は**授業開始の15分前から**業務が始まり、**2限のあとは食堂へ運ぶ15分**も仕事。
  つまり昼休みには「2限の後」「3限の前」の**2つの枠が別の人の仕事として並ぶ**
- 補充できない場合は教務課または支援室スタッフが対応する

### 人材プールについて
- 各スタッフのスケジュール（何曜日の何限が空いているか）は**本人がマイページで登録する**（D28/D32）。
  Googleフォームでの収集は廃止した（D39。一度も稼働させないまま、マイページが同じ役割を担った）
- **更新頻度**: 学期区分に合わせて再収集・上書き更新。**履修登録のタイミングに揃う**：
  春に「前期・1Q・2Q」、秋に「後期・3Q・4Q」を**まとめて登録**する（＝先端理工の1Q/2Qも春に同時登録）。
  そのため空きコマ再収集は実質**セメスターのリズム（年2回）**で、各回がその半期の2クォーターを含む。
  ※ 1Qと2Qで履修が変われば空きコマも変わりうる（available_slots を学期別に持つかは Phase 3 の検討事項）。
- クォーター制を導入しているのは**先端理工学部のみ**（学籍番号が **Y 始まり**。1年4クォーター。今後変更の可能性大）。
  **それ以外の学部はセメスター制（前期／後期）**。この2体系は同じ暦の上で同時に走り、**前期⊇1Q+2Q／後期⊇3Q+4Q**の入れ子。
  （例：7月は「前期」と「2Q」が両方"現在"）。
  → `terms` シート（学期マスタ）で開始/終了日から「今日を含む学期の集合」を解決する（decisions.md D16・`Terms.gs`）。
  対象学部の学期区分に応じて再収集サイクルが変わる点に注意

### イレギュラーパターン
- 勤務時間の超過
- 授業が予定より早く終了
- 授業の休校
- 欠員が出て予定スタッフが入らなかった
- 別のスタッフが代わりに入った（担当変更）

### 勤怠入力ルール（出勤簿）
- 授業が早く終わっても、**元々勤務予定だった時間分**を出勤簿に入力する
- 支払いは**15分単位**。遅刻・延長はシフト時間を15分単位で増減する
  （例：5分遅刻 → 予定時間 −15分、10分延長 → 予定時間 +15分）
- → **機能Bの照合設計の前提**：出勤簿は「予定ベース＋15分丸め」で記録されるため、
  Googleカレンダー（実態に寄せて手修正）とは必ずしも分単位で一致しない。
  照合は分一致ではなく15分単位の丸めとこのルールを織り込んで差分判定する

---

## 技術スタック（確定）

| 層 | 技術 | 理由 |
|---|---|---|
| フロントエンド | GAS HTML Service（HTML / CSS / JS） | サーバー不要・GASに同梱 |
| バックエンド | Google Apps Script（GAS） | Google API をコード1行で呼び出せる |
| データ保存 | SpreadsheetApp（GAS組み込み） | 大学Google管理下・職員が直接確認可能 |
| 認証 | GAS組み込み（大学Googleアカウント自動） | OAuth設定不要 |
| カレンダー連携 | CalendarApp（GAS組み込み） | 既存カレンダーをそのまま活用 |
| 通知 | Google Chat Incoming Webhook（スタッフ個人スペースに個別送信） | Gmail より確実に届く・Chat API Bot 不要で個別通知できる |
| ホスティング | GAS Web App（Googleサーバー） | サーバー管理・Docker・Nginx不要 |

**制約**: 外部データベース（Firebase・Supabase等）は使用不可。
データは大学Google Workspace管理下に完結させること。

---

## Sheetsデータ設計

**シート定義（列・意味・決まりごと）とER図は [`docs/spec/architecture.md`](docs/spec/architecture.md) が正。**
ここには写さない。同じ表を2か所に置くと必ずずれる（実際 2026-09 にずれた・D43）。

- **シートの列に触る作業の前に、必ず `docs/spec/architecture.md` を開くこと。**
  このファイルと違って自動では読み込まれないので、記憶で書くと間違える。
- 列を足したら **`architecture.md` の ER図** と **`tools/gas-harness.js` の `DEFAULT_HEADERS`** の
  両方を直す。
- スプレッドシートは2つ。**メインDB**（`staffs` / `courses` / `vacancies` / `responses` /
  `periods` / `terms`）と、**連絡先DB**（`contacts` / `registrations`・**職員のみアクセス可**）。
  どちらを開くかは `Sheets.gs` の `CONTACTS_DB_SHEETS` が決める。

### 特に壊れやすい決まりごと（詳細は architecture.md）

| 決まり | 外すとどうなるか |
|---|---|
| `periods` の**行順が時間割の並び順** | 並べ替えると崩れる。構築時に確定・運用では触らない（D40） |
| `periods.period` は**文字列キー**（`移動前3`） | `assist_slots` に `月移動前3` の形で入る。改名すると一致しなくなる |
| `skills` 空欄＝**全対応**（D9） | 空にすると全依頼が飛ぶ（「できません」の逆） |
| `available_slots` は**テイク用** | 介助は `assist_slots`（D32）。混同すると片方で候補0人 |
| 空の `staff_a_id` は**担当未定**（D38） | 各経路は空を除外済み。欠勤連絡には出ない（正しい） |

## ディレクトリ構成（現状）

GAS は src/ 一式（.gs と .html を同居）を clasp で push する。html は別ディレクトリではなく src 直下。
ファイル一覧は `ls src/` で確認すること（この節に写すと実態とずれる）。
`device/` は M5Stack、`docs/` は spec=仕様・設計 / guides=手順書 / history=変更履歴・レビュー。
`tools/` は開発用の検証スクリプト（Node）。`.clasp.json` の `rootDir` が `src` なので
**`clasp push` の対象外**で、本番の動作には関係しない（使い方は `tools/README.md`）。

### 画面（HTML）の共通ルール（D20）

- **ヘッダーとナビは各HTMLに書かない**。`code.js` の `renderChrome_` がサーバー側で組み立て、
  各画面は `<?!= chrome ?>` を1行貼るだけ。ナビの内容は `PAGES` と `NAV_PAGES` から導出される。
- **画面を追加するとき**は `PAGES` に足し、ナビに出すなら `NAV_PAGES` にも足す。それだけで全画面の
  ナビに載り、`staffOnly` による出し分け・現在地ハイライトも自動で効く（各HTMLの編集は不要）。
  - `staffOnly: true` … 職員限定（`doGet` がアクセスも拒否する）
  - `hideInStaffNav: true` … 職員のナビに出さないだけ（アクセスは拒否しない。例：`absence`）
- **CSSは `shared-styles.html` を先に読む**。`<?!= include('shared-styles') ?>` を画面固有の `<style>`
  より**前**に置くこと（後に書いたものが勝つため、画面固有で上書きできる順序にする）。
  色・余白・角丸は `:root` のCSS変数を使い、生の色コードを新たに書かない。
  本文幅は各画面が `:root { --main-max: 760px; }` のように指定する。
- 共通パーツは `.card` / `.btn`（`primary`/`ok`/`danger`/`block`/`sm`）/ `.notice`（`ok`/`warn`/`err`/`info`）/
  `.state` / `.hint`。画面固有の見た目だけを各HTMLの `<style>` に書く。

※ 勤怠整合性チェック（機能B）のコードは `future/feature-b/` にある（**`src/` には無い**・D41）。
  実カレンダーとの照合は一度も走らせていない。再開の手順と残課題は `future/feature-b/README.md`。

---

## 設定値の管理（PropertiesService）

GASでは `.env` の代わりに `PropertiesService` でスクリプトプロパティを管理する。
GASエディタの「スクリプトのプロパティ」から設定し、コードには直書きしない。

| プロパティキー | 内容 |
|---|---|
| SPREADSHEET_ID | メインDB スプレッドシートのID |
| CONTACTS_SPREADSHEET_ID | 連絡先DB スプレッドシートのID |
| CALENDAR_ID | 連携するGoogleカレンダーのID |
| CHAT_WEBHOOK_URL | 職員向け Chat スペースの Incoming Webhook URL（スタッフ個別の Webhook は連絡先DBで管理） |
| DEVICE_TOKEN | 物理アラート端末（M5Stack）用の認証トークン（D6）。端末スケッチと完全一致させる |

**.clasp.json はGitにコミットしない（スクリプトIDが含まれるため）。**

---

## 開発フェーズ

### Phase 1 - MVP（最優先）— 完了
- [x] GASプロジェクト作成・clasp設定
- [x] Sheetsデータ設計・テンプレート作成
- [x] 欠員登録フォーム（HTML Service）
- [x] 候補者への Google Chat 通知（Incoming Webhook）
- [x] 職員ダッシュボード（欠員一覧・回答状況）
- [x] 先着自動確定（D1）・候補0人の自動決着（D10）・再オープン
- [x] 利用学生中心の時間割デジタル化（D8）・マイビュー
- [x] スタッフのスキル区分（D9）・シフト入力画面（input）
- [x] 欠員補充管理に候補の電話番号表示（D11）

### Phase 2
- [ ] Google Calendar API連携（代行記録の自動反映）※運用は当面人手（D14：代行記録は手動）
- [—] 勤怠整合性チェック（機能B）※**今回のリリース対象外**。`future/feature-b/` へ退避（D41）
- [ ] 通知の非同期化・締切リマインド・エスカレーション（時間トリガー基盤・review #5/#7）
  - [x] 代行募集の締切を「授業開始30分前」に決定（`RECRUIT_DEADLINE_MIN_BEFORE`・D21）
  - [x] 締切超過の欠勤連絡は登録のみ受け付け、代行募集をしない＋Classroom案内（D21）
  - [x] **時間トリガー基盤**（`Triggers.gs`・`installTriggers()`／10分間隔）
  - [x] **締切到達時は募集クローズのみにし、`result` を書かず職員に決着を返す（D22）**。
    D10/D21 の「自動決着」を撤去した。**募集クローズ＝システム／決着＝職員** と用語を分けること
  - [ ] 締切**前**のリマインド（未回答の候補への再依頼）※同じトリガー基盤に乗せる
  - [ ] 通知の非同期化

### Phase 3
- [—] 月末リマインダー通知 ※機能Bとセット（D41）
- [—] 教務課向け照合レポート出力 ※機能Bとセット（D41）
- [ ] クォーター更新フロー（スタッフ空きコマ再収集）※入力画面・空きコマ反映は一部対応

---

## 作業上の注意事項

1. **認証情報は絶対にコードに直書きしない**。必ず `PropertiesService` を使うこと。
2. **データは大学Google Workspace内に完結**させること。外部サービスへの個人情報送信不可。
3. **職員が自走できるUI**を最優先。開発者がいなくなっても運用できること。
4. **実証実験フェーズでは実データを使わない**。仮想データで動作確認を行うこと。
5. Gitブランチは `main`（本番）と `dev`（開発）の2本運用。機能追加は `feature/xxx` ブランチで作業してPRを出すこと。
6. **GASの実行時間制限は6分/回**。重い処理は分割するか、時間起動トリガーで対応すること。
7. **push する前に手元で確かめる**（`tools/README.md`）。GASは構文エラーを「push して
   その関数を呼んだとき」にしか教えてくれず、HTML内のJSは画面を操作するまで気づけない。
   - **`node tools/run-all.js`** … 構文チェック＋画面の呼び出し先チェック＋全ロジック検証を
     まとめて実行（数秒）。
     `src/*.gs` を**そのまま**読み込み、Sheets・Notify・GAS API だけ差し替えてメモリ上で走らせる。
   - ⚠️ **HTML内のJSで未定義の関数を呼ぶと、画面が「読み込み中…」のまま固まる**（例外が
     握られて何も出ない）。`tools/check-pages.js` がこれを拾う。ヘルパーを別画面から
     持ってくるときは定義ごと持ってくること。
   - GAS側のテストは `src/Tests.gs`（疎通・診断）と `src/E2E.gs`（通し確認）に分離済み。
     ここに残すのは**GASでしか確かめられないもの**だけ（実LockServiceの競合・実Chat送信・
     実カレンダー・実PropertiesService・実シート書き込み）。
   - **本番ファイル（`Vacancy.gs` 等）にテスト関数を書き足さないこと。** ロジックなら
     `tools/sim-*.js`、GAS依存なら `src/Tests.gs` か `src/E2E.gs` に置く。
   - ⚠️ シートに列を足したら `tools/gas-harness.js` の `DEFAULT_HEADERS` も更新する。
8. **`clasp push` だけでは公開URL（`/exec`）に反映されない**。`/exec` が配信するのは
   「デプロイしたバージョン」で、push は HEAD を更新するだけ。**エラーは出ずに古い画面が出続ける**。
   実機確認の前に必ずデプロイを更新すること（`npx clasp deployments` で現在のバージョンを確認 →
   「デプロイを管理 → 鉛筆 → 新しいバージョン」。**新規デプロイは作らない**＝URLが変わるため）。
   検証中だけなら「デプロイをテスト」の `/dev` URL が常に最新を配信する（編集権限のあるアカウント限定）。
   詳細は `docs/guides/setup.md`。
9. **シートへのアクセスは必ず `Sheets.gs` の関数を経由**する（`readRows` / `findRow` / `updateRow` /
   `claimIfEmpty` 等）。`Sheets.gs` は1実行の間だけ読み取り結果をキャッシュしており、整合性は
   「ロックを跨いだら捨てる」（`withLock_`）で担保している。この前提を壊さないため：
   - 直接 `SpreadsheetApp` で書き換えたら、その後に `invalidateSheetCache_()` を呼ぶ（`Setup.js` の各 migrate が例）
   - `withLock_` の中で「書き込み対象と**別**のシート」を読んで書き込みを判断しない
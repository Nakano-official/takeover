# 開発の経緯（変更履歴）

龍谷大学 障がい学生支援室 シフト管理・業務DXシステムの開発を、日付ごとに「何を・どう変えたか」でまとめたもの。
仕様の根拠は決定ログ（[`decisions.md`](decisions.md)）、設計は [`architecture.md`](architecture.md)、運用は
[`operations.md`](operations.md) を参照。括弧内の `D1` 等は決定ログ、`#1` 等は [`review-2026-06-23.md`](review-2026-06-23.md) の指摘番号。

---

## 2026-06-10 プロジェクト立ち上げ

- リポジトリを初期化し、**要件定義**と GAS セットアップ方針を整備。
- 技術スタックを確定：フロント＝GAS HTML Service、バックエンド＝Google Apps Script、データ＝Spreadsheet、
  通知＝Google Chat Webhook、ホスティング＝GAS Web App。外部DBは使わず大学 Google Workspace 内に完結させる方針。

## 2026-06-11 設計ドキュメント

- **データ設計**と**システムアーキテクチャ**を文書化（`architecture.md`）。
- 画面一覧・データフロー・スプレッドシート構成（メインDB／連絡先DBの2分離）・URL設計（`?page=` 振り分け）を定義。

## 2026-06-12 基盤実装

- **初回セットアップスクリプト**（スプレッドシート生成）を追加。
- **共通シート操作レイヤー `Sheets.gs`** を実装。全データアクセスをここに集約し、ヘッダー名で読む（列順非依存）方針に。
- 個別通知設計・アクセス制御設計を追記（学生にシート権限を渡さず、GAS を「職員として実行」＋ロール判定で守る）。

## 2026-06-14 機能A（欠員補充）の骨格

- **Web App エントリポイント `code.js`**（`doGet` ルーティング・ロール判定）。
- **シフト確認（home）／欠勤連絡（absence）／回答（respond）／欠員補充管理（manage）** の各画面を追加。
- **Google Chat 通知 `Notify.gs`**：欠員発生時に職員スペース＋候補者へ個別通知。
- 未対応の改善点を `backlog.md` に記録。

## 2026-06-16 相談反映・周辺機能

- アドバイザー相談・本部確認の決定を `decisions.md` に記録（カレンダー予定フォーマット **D7** など）。
- 方針を**先着自動確定**へ更新（**D1** の前提を整理）。
- 勤怠CSVの実仕様を記録し、**実データCSVを Git 管理外**に（個人情報の混入防止）。
- **カレンダー診断ツール（`Calendar.gs`）** を追加し、予定フォーマットを実データで実証（機能Bの基盤）。
- 個別通知のスケール課題と簡略化案を `notify-scaling-options.md` に整理。
- **欠員アラート端末（M5Stack）用 GAS エンドポイント（D6）** とスケッチを実装（件数＋最新連番のみ返し個人情報を出さない）。

## 2026-06-23 機能Aの作り込み・時間割デジタル化・レビュー対応

この日に大きく前進した。テーマ別に記す。

### 欠員補充の中核（D1）
- 代行を**先着自動確定**に変更。最初に「承諾」した候補へ `LockService` 内の compare-and-set（`claimIfEmpty`）で
  原子的に確定し、同時承諾の競合を防止。埋まったら回答画面に「受付終了」を表示。

### 利用者中心の時間割をアプリ内にデジタル化（D8）
- `courses` を「利用者×曜日×時限」の構造へ拡張（`support_type`/`user_student`/`subject`/`instructor`/`room`/`note`）。
- シフト確認を**時間割ビュー**に刷新（PC＝グリッド／スマホ＝曜日タブのレスポンシブ）。
- **絞り込み**（利用者／スタッフ＝自分／内容）、コマの**ボタン化＋詳細モーダル**、**教室の常時表示**、
  **軸を固定枠化**（空き時限・空きセルも表示）、テイク1名のコマに**「あと1名 募集中」緑表示**。
- **マイビュー**：自分の担当コマと「自分が入れる募集中」を同じ時間割で見比べ（D8③）。
- 動作確認用にダミーデータを増量（月〜金・利用者8・学生12、スキル混在）。

### スタッフのスキル区分（D9）
- `staffs` に `skills`（テイク／介助／両方）列を追加。**DBは分割せず**単一の `staffs` を維持。
- 代行候補抽出を**内容一致で絞り込み**（介助のみの人にテイク依頼が飛ばない）。

### シフト入力画面（backlog #4）
- 職員が `courses` を画面から追加・削除できる **`input` 画面**を実装。
- 利用者を**選択式**（新規追加可）に、担当を**「空き×スキル」で自動絞り込み**して職員の手作業を削減。

### 本番運用・ドキュメント整備
- 本番用 **`setupSpreadsheetsEmpty`**（空のDB生成）を追加し、デモ（ダミー）と分離。
- **README を再構成**（画面一覧表・折りたたみ詳細）、**Mermaid 図**（構成・欠員補充フロー・ER図）を追加。
- **運用手順書を職員向け（URL受領後）に限定**し、構築手順は **`setup.md`** に分離。所有アカウントの引き継ぎ注意も明記。

### コードレビュー対応（review-2026-06-23.md）
- **#1 ダブルブッキング除外**：候補抽出で「同クォーター・同スロットの別コマ担当」「当日・同スロットで代行確定済み」を除外。
- **#2 欠勤日検証**：形式・過去日・コマ曜日との一致をチェック。
- **#3 最新クォーター限定**：`getMyCourses` が古い学期のコマを選択肢に出さないように。
- **#8 手動決着の通知**：「職員対応／1人テイク」「手動確定」時も候補・関係者へ通知（先着確定と対称化）。
- **#4 候補0人の自動決着（D10）**：補充候補がいなければ、相方が残れば「1人テイク」、残らなければ「職員対応」に自動で倒す。
- **再レビュー対応**：確定済み欠員を戻せる**「再オープン」**導線（`reopenVacancy`）、両者欠勤時の判定補正、
  再オープン時に旧確定者へ**解除通知**。
- **電話フォローの導線（D11）**：欠員補充管理に**代行候補の電話番号・回答状況**を表示し、返信が来ないとき
  職員が電話 → 口頭OKなら画面内で確定できるように（連絡先DB＝職員のみから取得）。#6 を電話で緩和。

## 2026-06-24 性能改善・ドキュメント整理

- **性能改善**：`getVacanciesForManage` の候補抽出を `ctx` 注入で N+1 リード解消（シート読込を一定回数に）。
  `findCandidates_` は `ctx`（staffs/courses/vacancies）省略時は従来どおり内部読込する後方互換。
- **ドキュメント現状化**：`requirements.md` に課題1・2の As-Is/To-Be フロー図（Mermaid）を追加し
  バージョン 1.2 へ。Phase 1（MVP）完了を反映。課題2 を実態（学生の労務管理システム記入ミス・漏れが主因）に再定義。
- **経緯整理**：本 `changelog.md` を新設し README からリンク。`claude.md`・`backlog.md` を実装に追従。
- **コマの編集機能（D12）**：`input` 画面に登録済みコマの**編集**を追加（`updateCourse`）。一覧の「編集」で
  入力フォームを更新モードに切り替え。背景は、科目・教員・教室が**履修登録PDFの職員手入力**で、
  履修登録時点では**教室が未定のことがある**ため後から直せるようにした。欠員が紐づくコマは
  曜日・時限・内容の変更を禁止（科目・教室・担当などは可）。

### フォーム連携（空きコマ・連絡先の自動取り込み）
- 学生回答フォーム（①空きコマ・スキル ②電話・Webhook URL）を送信トリガー（`onIntakeFormSubmit`）で
  `staffs`・`contacts` へ自動反映する **`Forms.gs`** を追加。回答先は連絡先DB（職員のみ）にリンクし、
  **メールをキーに名簿照合**して登録済みスタッフの行だけ更新（学外・無関係ユーザーの混入を防止）。
- 設置は `installIntakeTrigger()` を1回手動実行。`testIntakeMapping()` で設問→値のマッピングを確認。
- レビュー対応：**skills は空送信で上書きしない**（空欄＝全対応の誤判定で介助専門者にテイク依頼が飛ぶのを防止）、
  **Webhook は Google Chat の URL 形式のときだけ反映**（誤URL・別宛先の混入防止）。

## 2026-06-30 機能B（勤怠整合性チェック）の実装（D14）

- **照合エンジン `Attendance.gs` ＋ 職員画面 `check.html`** を実装。勤怠CSVをアップロード→Shift_JIS復号→
  カレンダー（職員が修正済みの「予定の正」）と **15分丸め**で突合し、**登録漏れ／予定外勤務／時間不一致**を
  ハイライトする。CSVは**メモリ内照合のみでシートに保存しない**（他キャンパス等の個人情報を残さない・D2/D14）。
- 突合キーとして `staffs` に **`personal_code` 列**（勤怠CSVの「個人ｺｰﾄﾞ」）を追加（`migrateStaffsPersonalCode`）。

## 2026-07-09 全体レビューと確定処理の原子化

- **ドキュメント再編**：`docs/` を **spec（仕様・設計）/ guides（手順書）/ history（記録）** の3フォルダに整理。
- **8角度コードレビュー**（[`review-2026-07-09.md`](review-2026-07-09.md)）：機能A・B全体を対象に確認済み問題10件を
  抽出し、修正手順を `backlog.md` §10 に、設計の理想形を §11、運用の未確認事項を §12 に記録。
- **10-1 確定処理の原子化（D1違反の是正）**：職員側の `confirmSubstitute` / `setVacancyResult` を
  「確認→更新」の2段階から **`claimIfEmpty`（compare-and-set）** に置き換え、確定中に候補者が先着確定する
  競合での**二重確定を防止**。`reopenVacancy` も汎用CAS **`updateRowIfGuard_`**（非空のときだけ戻す）に統一し、
  旧確定者の控えとクリアを同一ロックで行うようにした。競合分岐は合成データで検証（19ケース）。

## 2026-08-09 UIの共通化とデザイン刷新（D20）

- **共通レイアウトの一元化**：7画面がヘッダー・ナビ・CSS を各自コピーしており、実際にナビの内容が
  画面ごとにズレていた（`home`/`absence`/`respond` に「学期設定」が無い、`terms` に「欠勤連絡」が無い、
  `manage`/`check` に「シフト入力」が無い、現在地表示も無し）。
  ヘッダー＋ナビを **`code.js` の `renderChrome_`** がサーバー側で生成する形に変え、各HTMLは
  `<?!= chrome ?>` を貼るだけにした。ナビの中身は **`PAGES` と新設 `NAV_PAGES` から導出**するので、
  画面を足せば全ナビに自動で載り、`staffOnly` による出し分けも `doGet` と同じ判定で自動に効く。
- **デザイントークン**：`shared-styles.html` を新設し、色・余白・角丸・影を `:root` のCSS変数に集約。
  共通パーツ（`.card`/`.btn`/`.notice`/`.state`/`.hint`）も同居させ、各画面は `include('shared-styles')` で読む。
  ブランド色を `#4a86e8` → `#2b5fd9` に深め、ナビを sticky 化。フォーカスリング（キーボード操作時のみ）を追加。
  **HTML 7画面で 426行削除 / 314行追加**。
- **職員ナビから「欠勤連絡」を削除**：`hideInStaffNav` を新設。欠勤連絡は学生スタッフが自分の担当コマの
  欠勤を出す画面で、職員には担当コマが無く常に空になるため。アクセス制限ではなくナビの出し分けのみ。
- **ナビの大きさを利用実態で出し分け**：学生はスマホ中心・職員はPC中心のため、**3項目以下**（学生）または
  **PC幅（761px以上）**では大きな切り替えボタン、それ以外（職員のスマホ）は横スクロールのピル並びにした。
- **スマホの絞り込みを折りたたみ**（`home`）：絞り込み4項目が常時開いていて時間割が画面下へ押し下げられて
  いたため、スマホでは既定で閉じるようにした。**マイビューは折りたたみの外**に出し（学生が最も使う切り替えの
  ため1タップを維持）、閉じていても絞り込み中と分かるよう**条件数のバッジ**を出す。PCは従来どおり常時表示。

## 2026-08-09 バックエンドの往復削減と通知の一括送信（backlog 10-10）

機能Aの中核である**先着自動確定（D1）が、競合が起きるまさにその瞬間に最も遅い**という状態の解消。
候補者が「承諾」を1タップしたときのサーバー処理を計測すると、`respondToVacancy` →
`isCandidate_` → `notifyVacancyFilled` の経路だけで **スプレッドシートを19回開き、19回全読みし、
Chat通知を3回逐次送信**していた（候補が増えるほど contacts の全読みと fetch が線形に増える）。
ロック待ちは `LOCK_TIMEOUT_MS = 15秒` なので、複数候補が同時に承諾する状況ほど危うい。

- **実行内キャッシュ（`Sheets.gs`）**：1実行の間だけ、Spreadsheet/Sheet オブジェクト（`openById` の往復）と
  シートの生データ（headers + values）をメモ化する。整合性は「**ロックを跨いだら必ず捨てる**」の一点に集約し、
  `withLock_` がロック取得直後と解放直前に `invalidateSheetCache_()` を呼ぶ。これにより
  (a) ロック内の読み取り（`nextId_` の採番・CASの現在値検証）は必ず実データを見る
  (b) 自分の書き込みは次の読み取りに必ず反映される、が保証される。
  キャッシュに乗るのは**ロック外の読み取りだけ**で、書き込み判断には一切使われない。
  `readRows` は生の値配列だけをキャッシュし、オブジェクトは毎回組み直すので呼び出し側は戻り値を自由に扱える。
- **通知の一括送信（`Notify.gs`）**：候補ごとの `getStaffWebhook_` → contacts 全読みを
  `buildWebhookMap_()` の**1回読み**に置換。逐次 `UrlFetchApp.fetch` を **`UrlFetchApp.fetchAll`** に置き換え、
  職員スペース・確定者・他候補への送信を**1回**にまとめた（`postToWebhooks_`）。
  空・不正なURLは fetchAll に渡さず「Webhook未登録」として記録する（1件の不正URLで全員に届かなくなるのを防ぐ）。
- **行更新の書き込み（`Sheets.gs`）**：列ごとの `setValue` ループを `writeRowUpdates_` に集約し、
  **連続する列はまとめて `setValues`** する。更新対象外のセルには触れないので、他列の書式・数式は巻き込まない。
- **計測（合成シミュレーション・36アサート通過）**：主要なサーバー関数での API 往復（候補3名のデータセット）。
  **スプレッドシートを開く回数はどの操作でも2回（メインDB＋連絡先DB）に固定**される。

  | 操作 | DB open | シート全読み | HTTP送信 |
  |---|---|---|---|
  | 代行依頼に承諾（`respondToVacancy`） | 19 → **2** | 19 → **11** | 3 → **1** |
  | 欠勤連絡を出す（`submitAbsence`） | 21 → **2** | 21 → **12** | 3 → **1** |
  | 再オープン（`reopenVacancy`） | 19 → **2** | 19 → **9** | 4 → **2** |
  | 職員が手動確定（`confirmSubstitute`） | 13 → **2** | 13 → **8** | 3 → **1** |
  | 回答画面を開く（`getVacancyForRespond`） | 10 → **2** | 10 → **6** | – |
  | 時間割を開く（`getTimetable`） | 9 → **2** | 9 → **6** | – |
  | 欠員補充管理を開く（`getVacanciesForManage`） | 8 → **2** | 8 → **6** | – |
  | 欠勤対象コマの取得（`getMyCourses`） | 7 → **2** | 7 → **5** | – |

  候補人数に対する伸びも解消した（従来は候補1人ごとに contacts を1回全読み＋fetch 1回で線形増加。
  現在は contacts 1回読み＋fetchAll 1回で一定）。
  あわせて、CASの割り込み検知（他候補が先に確定 → 上書きせず「受付終了」）、ロック外の古い読み取りからの
  **ID二重採番が起きないこと**、書き込み直後の読み取りが最新を返すことを検証した。

## 2026-08-09 代行募集の締切と直前欠勤の扱い（D21・requirements §8 の一部決定）

`requirements.md §8` に「回答締切・全員辞退時の運用」が未決のまま残っていた（review #7）。
締切が無いため回答の来ない欠員は当日まで放置され、逆に「授業5分前の欠勤連絡」に対しては
**現実的に間に合わない代行依頼**が候補へ飛んでいた。ヒアリングで締切と直前欠勤の扱いが決まったので実装した。

- **締切＝授業開始30分前**（`Constants.gs` の `RECRUIT_DEADLINE_MIN_BEFORE`）。開発者が変更する定数として置く。
  締切は欠員行に**保存せず** `vacancies.date` ＋ `periods.start_time` から毎回算出するので、
  定数を変えれば既存の未解決欠員にも即反映される。
- **締切を過ぎた欠勤連絡（直前欠勤）**：`submitAbsence` に分岐を追加。**欠勤の登録は受け付ける**が
  代行候補への依頼は送らず、職員スペースへ「⏰ 直前の欠勤連絡です（代行募集なし）」を通知し、
  D10 と同じ分岐で自動決着させる（相方が残れば「1人テイク」／残らなければ「職員対応」）。
  学生の画面には「代行の募集は行いません。お手数ですが Google Classroom にも欠勤の連絡を投稿してください」を出し、
  システムが担えない部分を**従来の人力フローへ明示的に差し戻す**。
- **共通化**：候補0人の自動決着（D10）と直前欠勤の決着で重複していた「相方が残るか」の判定を
  `autoSettleResult_` に抽出。`notifyAutoResolved` は `reason` を受け取って文面を出し分ける。
- **フォールバック**：時限マスタに開始時刻が無い／読めない場合は締切判定をせず、従来どおり募集する（安全側）。
- **検証**：合成シミュレーション55アサート通過。締切前後（08:00 / 08:45）・**境界**（08:30 ちょうどは募集する、
  08:31 は募集しない）・開始時刻が空のときのフォールバックを含む。
- **実機テストで見つかった不具合（periods の時刻セルが Date 化する）**：`e2eDeadlineFlow` の初回実行で
  締切判定が**常に「締切前」に倒れる**ことが判明した（14件中8件失敗）。原因は `periods` シートの
  テキスト書式が**既定の7行ぶんにしか掛かっていない**こと（`getRange(2,1,7,3).setNumberFormat('@')`）。
  8行目以降に時限を足すと `'09:15'` が Sheets に**時刻値**として保存され、`readRows` が Date を返し、
  `HH:mm` 文字列しか想定していなかった `isPastRecruitDeadline_` がパースに失敗していた。
  **10-6（personal_code の書式が1セルのみ）と同じパターン**で、職員が8限や課外時限を手で足した瞬間に
  本番でも起きる。しかもエラーにならず、直前欠勤が全部「通常募集」として処理されるだけなので気づけない。
  - **対処1（堅牢化）**：`periodStartMinutes_` を新設し、文字列と Date の**両方**を受ける。
    パースは既存の `hhmmToMin_`（文字列）と `jstMinutes_`（Date）を使い分ける。秒つき・全角コロンも許容。
  - **対処2（根本）**：`setupMainDb_` の periods を**列全体**テキスト固定に変更。既存DB向けに
    `migratePeriodsTextFormat()` を追加（既に時刻値の行があれば一覧で警告する）。
  - **再実行の結果**：全14件成功。本番の1〜7限は `string` のままで表示系に影響なし、
    Date 型の行でも締切判定が正しく働くことを実機で確認した（`16:53` → `1013分`）。
- **実機確認用**：`Vacancy.gs` に `e2eDeadlineFlow()` を追加（**一時的な確認用。確認が済んだら削除してよい**）。
  本物の授業時刻を待たずに済むよう、**時計ではなく授業時刻の方を動かす**（開始時刻＝今から+N分の一時時限を作る）。
  実行アカウント自身を担当スタッフにした一時コマに対して `submitAbsence` を通し、最後に一時データを全削除する。
  既定では `CHAT_WEBHOOK_URL` を一時的に外して実送信を止める（`{notify:true}` で実際に送る）。
- **未実装**：締切**前**の回答リマインドと、締切到達時の自動エスカレーションは時間トリガー基盤が要るため見送り
  （Phase 2）。リマインドのタイミング・回数、夜間/休日の送信可否も未決のまま。

## 2026-08-24 時間割に週の軸を入れ、欠員を日付で表示する（D23・backlog #6）

欠勤連絡は「いつの授業を休むか」を必ず持ち、`submitAbsence` は形式・過去日・曜日一致・
学期の開講期間内まで検証して `vacancies.date` に記録している。ところが表示側の `getTimetable` は
`course_id` だけで欠員を畳み、行順の最後の1件を採用していた。**入力側が丁寧に扱った日付を
表示側が捨てていた**ため、ひと月前の代行が今も「補充済 → 氏名」と出続ける状態だった。

- **週の軸**：時間割に月曜起点の週ナビ（◀ 前の週／今週へ／次の週 ▶）を追加。各コマの欠員は
  「その週のその曜日の日付」に一致する `vacancies` **だけ**で判定する。別の週の欠員を引きずらない。
- **誰の代わりに誰が**：日付が定まったので欠勤者も返せるようになり、バッジが「担当A → 田中」の形になった。
  詳細モーダルにも「この週の日付／この日の欠勤／この日の代行」を出す。
- **曜日の枠は月〜日**：週の途中が抜けているのは不自然なため土日も枠として出す（日曜はほぼ空欄）。
  シフト入力の選択肢は `WORK_DAYS`（月〜金・D17）のまま。
- **開講期間の反映**：表示中の週のその日が `terms` の期間外なら、そのコマは出さない。
  判定はコマ単位・日単位なので、期間の途中で切れる週は**曜日ごとに分かれる**
  （後期の終了が木曜なら月〜木は授業あり・金〜日は空欄）。期間未登録の学期は絞らない（D16 と同じ後方互換）。
  期間外で空になったときは「この週は授業期間外です」と明示し、絞り込みで空になった場合と区別する。
  件数表示の分母も「この週に授業があるコマ数」に直した。
- **学期境界の但し書き**：`terms` は1学期に開始日・終了日を1組しか持てず、曜日ごとのズレ
  （水曜始まり・GW の振替など）を表現できない。`Constants.gs` の `TERM_BOUNDARY_NOTICE` を
  唯一の出所として、時間割と欠勤連絡の2画面に「実際の授業日はご自身の履修時間割で確認してください」を明記。
  **祝日判定は入れない**（私立で祝日にも授業があり、推測するほど壊れやすくなるため）。
- **週送りは往復ゼロ**：週で変わるのは欠員の重ね合わせだけなので、`getTimetable` は週を決め打ちせず
  `vacancy`（`course_id|日付` の索引）と `termRange` をまとめて返し、週の切り替えはクライアントで完結させる。
  「今日」の判定も端末の時計ではなくサーバーの JST（`today`）を使う。
- **テスト可能性**：週の中核ロジックを DOM に触らない純関数 `weekStateOf` として切り出し、
  検証ハーネスが `home.html` から直接取り出してテストする形にした。
- **検証**：合成シミュレーション99アサート通過。週の丸め（日曜指定で翌週にずれない）・月/年またぎ・
  開講期間の境界（終了日当日は授業あり／翌日はなし／同じ週でも曜日で分かれる）・欠員の週別表示を含む。
- **実装で1点**：`selDay()` がタブのラベル文字列で曜日を判定していたため、見出しに日付を足すと
  スマホの曜日タブが動かなくなる。`data-day` 属性による判定に変更した。

---

## 現状と次の予定（2026-06-24 時点）

- **機能A（欠員補充）** は通常フロー＋異常系（候補0人・電話フォロー・再オープン）まで整い、実証実験に進める品質。
- **保留中**：通知の非同期化（#5）／Webhook未登録の本格フォールバック（#6）／締切の自動エスカレーション（#7）は、
  `vacancies` への作成時刻追加＋**時間トリガー基盤**でまとめて対応予定。
- **機能B（勤怠整合性チェック）** は `Calendar.gs` の診断のみ＝Phase 2 として未実装。

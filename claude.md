# ryukoku-support-shift — Claude Code コンテキスト

このファイルはClaude Codeがプロジェクトを理解するためのコンテキストです。
作業を始める前に必ずこのファイルを読んでください。

> **あわせて参照**：相談で決まった仕様・経緯は `docs/decisions.md`（決定ログ）、
> 未対応の改善点は `docs/backlog.md` にある。仕様の判断はこの2つも確認すること。

---

## プロジェクト概要

**龍谷大学 障がい学生支援室 シフト管理・業務DXシステム**

障がい学生支援室のアルバイトスタッフ（ノートテイカー）のシフト管理、
欠員補充業務の自動化、および勤怠整合性チェックを行うWebシステム。

- **リポジトリ名**: ryukoku-support-shift
- **可視性**: Private
- **開発期間**: 〜 2026年9月17日（授業開始日）

---

## 解決する課題

### 課題1：欠員補充フローの非効率

現状フロー：
1. 学生スタッフが Google Classroom に欠勤連絡を投稿
2. **問題**: バナー通知に本文なし・イベント投稿に埋もれる（Pull型）
3. 職員が気づいた時点で人材プールのスケジュール表を目視でスクリーニング
4. 職員が候補者に個別電話
5. 全員断った場合 → 残り1人が単独テイク（例外）/ 最終手段で職員が対応

**原則**: テイク業務は常に2人体制。1人欠員 = 必ず補充アクションが必要。

### 課題2：勤怠整合性チェックの手作業

現状フロー：
1. クォーター開始時に全勤務予定をGoogleカレンダーに登録
2. イレギュラー発生時（超過・早退・休校・欠員・代行・担当変更）に
   本人 or 職員がGoogleカレンダーを手動修正 → **入力漏れが多発**
3. 月末に学生スタッフが大学ポータルで勤怠登録（期限：月末）
4. 支援室職員が「勤怠一覧データ ↔ Googleカレンダー」を**目視で突き合わせ**
5. 教務課職員が同じ作業をもう一度実施して承認

**問題の本質**: カレンダーと勤怠登録が別々に存在し、人間が手動照合している。

---

## システムが解決すること（機能2本柱）

### 機能A：欠員補充の自動化
- 欠員情報をPush通知で職員・スタッフに即時伝達
- 人材プールから該当コマの空きスタッフを自動スクリーニング
- 候補者へ一括で代行依頼通知を送信（Google Chat Webhook）
- 回答を集約してダッシュボードに表示
- **最初に「承諾」した候補者へ先着順で自動確定**（職員は通常介在しない）。
  誰も承諾しない等の例外時のみ、職員が「1人テイク／職員対応」で決着させる。
  ※ 同時承諾の競合に備え、確定処理は LockService 内で原子的に行う（docs/decisions.md D1）
- 代行記録をGoogleカレンダーに自動反映

### 機能B：勤怠整合性チェックの自動化
- Googleカレンダーの勤務予定と勤怠登録データを自動照合
- 差分（登録漏れ・時間不一致など）を一覧でハイライト表示
- 月末前にリマインダー通知を自動送信
- 照合済みレポートを教務課と共有、承認ボタン1つで完結

---

## 業務ドメイン知識

### テイク業務について
- ノートテイク業務は**原則2人体制**
- 1人が欠員 → 補充が必要（補充できない場合のみ例外的に1人テイク）
- 1人体制のテイクに欠員 → 補充できない場合は最終手段で職員が対応

### 人材プールについて
- 各スタッフのスケジュール（何曜日の何限が空いているか）をフォームで収集
- **更新頻度**: クォーター毎（年4回）に再収集・上書き更新
- 龍谷大学はクォーター制（1年4クォーター）

### イレギュラーパターン
- 勤務時間の超過
- 授業が予定より早く終了
- 授業の休校
- 欠員が出て予定スタッフが入らなかった
- 別のスタッフが代わりに入った（担当変更）

---

## 技術スタック（確定）

| 層 | 技術 | 理由 |
|---|---|---|
| フロントエンド | GAS HTML Service（HTML / CSS / JS） | サーバー不要・GASに同梱 |
| バックエンド | Google Apps Script（GAS） | Google API をコード1行で呼び出せる |
| データ保存 | SpreadsheetApp（GAS組み込み） | 大学Google管理下・職員が直接確認可能 |
| 認証 | GAS組み込み（大学Googleアカウント自動） | OAuth設定不要 |
| カレンダー連携 | CalendarApp（GAS組み込み） | 既存カレンダーをそのまま活用 |
| 通知 | Google Chat Incoming Webhook（スタッフ個人スペースに個別送信） | Gmail より確実に届く・Chat API Bot 不要で個別通知できる |
| ホスティング | GAS Web App（Googleサーバー） | サーバー管理・Docker・Nginx不要 |

**制約**: 外部データベース（Firebase・Supabase等）は使用不可。
データは大学Google Workspace管理下に完結させること。

---

## Sheetsデータ設計

スプレッドシートは2つに分離する（詳細は docs/requirements.md・docs/architecture.md）。

- **メインDB**：以下のシート群。アクセスは職員 + GAS
- **連絡先DB**：contacts シート（staff_id・氏名・メール・電話番号・webhook_url）。**職員のみアクセス可**

### `staffs` シート
| カラム | 内容 |
|---|---|
| staff_id | スタッフID |
| name | 氏名 |
| role | 職員 / 学生（画面のアクセス制御に使用） |
| skills | 対応可能な内容：`テイク` / `介助` / `テイク,介助`（D9）。空欄は全対応扱い |
| available_slots | 空きコマ。`月1,月2,火3` 形式（クォーター毎に上書き） |

### `courses` シート
「利用者中心の時間割」を表す（decisions.md D8）。1行＝1コマ（利用者×曜日×時限）。
同じ曜日・時限に複数の利用者が並ぶ。テイク＝スタッフ2名、介助＝1名（`staff_b_id` 空）のことがある。

| カラム | 内容 |
|---|---|
| course_id | コマID |
| quarter | クォーター（例：2026-Q3）。過去分は削除しない |
| day | 曜日 |
| period | 時限 |
| support_type | 内容：テイク / 介助 |
| user_student | 利用学生（被支援者）の氏名 |
| subject | 科目名 |
| instructor | 担当教員 |
| room | 教室 |
| staff_a_id | 担当スタッフA |
| staff_b_id | 担当スタッフB（介助で1名なら空） |
| note | 備考 |

### `vacancies` シート
| カラム | 内容 |
|---|---|
| vacancy_id | 欠員ID |
| date | 発生日 |
| course_id | 対象コマID |
| absent_staff_id | 欠員スタッフID |
| notify_status | 通知状況 |
| result | 補充済 / 1人テイク / 職員対応（空＝未解決） |
| substitute_staff_id | 確定した代行者のスタッフID（先着確定・手動確定で記録） |

### `responses` シート
| カラム | 内容 |
|---|---|
| vacancy_id | 対象の欠員ID |
| staff_id | 回答した候補者 |
| answer | 承諾 / 辞退 |
| answered_at | 回答日時 |

### `periods` シート（時限マスタ）
| カラム | 内容 |
|---|---|
| period | 時限（1〜7） |
| start_time | 開始時刻 |
| end_time | 終了時刻 |

---

## ディレクトリ構成（現状）

GAS は src/ 一式（.gs と .html を同居）を clasp で push する。html は別ディレクトリではなく src 直下。

```
ryukoku-support-shift/
├── src/                        # GAS スクリプト＋画面
│   ├── code.js                 # エントリポイント（doGet・ルーティング・ロール判定）
│   ├── Vacancy.gs              # 欠員補充ロジック（候補抽出・先着確定・自動決着・再オープン）
│   ├── Input.gs                # シフト入力（courses 追加・削除・候補絞り込み）
│   ├── Notify.gs               # Google Chat 通知
│   ├── Calendar.gs             # カレンダー連携（機能Bの基盤・診断のみ）
│   ├── Device.gs               # M5Stack 端末用エンドポイント
│   ├── Sheets.gs               # Sheets操作（共通）
│   ├── Setup.js                # DB初期化・ダミーデータ生成・マイグレーション
│   ├── home.html               # シフト確認（時間割）
│   ├── absence.html            # 欠勤連絡
│   ├── respond.html            # 代行依頼への回答
│   ├── input.html              # シフト入力（職員）
│   ├── manage.html             # 欠員補充管理（職員）
│   └── appsscript.json         # GASマニフェスト
├── device/                     # M5Stack スケッチと手順
├── docs/                       # 設計・運用ドキュメント
├── .clasp.json                 # clasp設定（GitIgnore対象）
└── CLAUDE.md                   # このファイル
```

※ 勤怠整合性チェック（機能B）の `Attendance.gs` と `check.html` は未実装（Phase 2）。`input`/`check` ルートは
  `code.js` に予約済み（`check` は「準備中」表示）。

---

## 設定値の管理（PropertiesService）

GASでは `.env` の代わりに `PropertiesService` でスクリプトプロパティを管理する。
GASエディタの「スクリプトのプロパティ」から設定し、コードには直書きしない。

| プロパティキー | 内容 |
|---|---|
| SPREADSHEET_ID | メインDB スプレッドシートのID |
| CONTACTS_SPREADSHEET_ID | 連絡先DB スプレッドシートのID |
| CALENDAR_ID | 連携するGoogleカレンダーのID |
| CHAT_WEBHOOK_URL | 職員向け Chat スペースの Incoming Webhook URL（スタッフ個別の Webhook は連絡先DBで管理） |
| DEVICE_TOKEN | 物理アラート端末（M5Stack）用の認証トークン（D6）。端末スケッチと完全一致させる |

**.clasp.json はGitにコミットしない（スクリプトIDが含まれるため）。**

---

## 開発フェーズ

### Phase 1 - MVP（最優先）— 完了
- [x] GASプロジェクト作成・clasp設定
- [x] Sheetsデータ設計・テンプレート作成
- [x] 欠員登録フォーム（HTML Service）
- [x] 候補者への Google Chat 通知（Incoming Webhook）
- [x] 職員ダッシュボード（欠員一覧・回答状況）
- [x] 先着自動確定（D1）・候補0人の自動決着（D10）・再オープン
- [x] 利用者中心の時間割デジタル化（D8）・マイビュー
- [x] スタッフのスキル区分（D9）・シフト入力画面（input）
- [x] 欠員補充管理に候補の電話番号表示（D11）

### Phase 2
- [ ] Google Calendar API連携（代行記録の自動反映）
- [ ] 勤怠整合性チェック（カレンダー ↔ 勤怠データ照合）※`Calendar.gs` に診断のみ
- [ ] 差分ハイライト一覧表示
- [ ] 通知の非同期化・締切リマインド・エスカレーション（時間トリガー基盤・review #5/#7）

### Phase 3
- [ ] 月末リマインダー通知
- [ ] 教務課向け照合レポート出力
- [ ] クォーター更新フロー（スタッフ空きコマ再収集）※入力画面・空きコマ反映は一部対応

---

## 作業上の注意事項

1. **認証情報は絶対にコードに直書きしない**。必ず `PropertiesService` を使うこと。
2. **データは大学Google Workspace内に完結**させること。外部サービスへの個人情報送信不可。
3. **職員が自走できるUI**を最優先。開発者がいなくなっても運用できること。
4. **実証実験フェーズでは実データを使わない**。仮想データで動作確認を行うこと。
5. Gitブランチは `main`（本番）と `dev`（開発）の2本運用。機能追加は `feature/xxx` ブランチで作業してPRを出すこと。
6. **GASの実行時間制限は6分/回**。重い処理は分割するか、時間起動トリガーで対応すること。